import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("📔 My Journal")
                    .font(.largeTitle)
                Spacer()
            }
        }
    }
}

#Preview {
    ContentView()
}
